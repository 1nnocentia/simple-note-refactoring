import 'package:flutter/material.dart';
import '../widgets/note_form_widget.dart';

class AddNote extends StatelessWidget {
  const AddNote({super.key});

  @override
  Widget build(BuildContext context) {
    return const NoteFormWidget();
  }
}
